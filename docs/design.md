# 🎞️ GifCreater 动画工坊 (v2.0 终极演进版) 产品与技术规格说明书

## 一、 产品定位与核心设计哲学

从原本单一的“16帧切片脚本”，全面进化为面向创作者与普通用户的**一体化多帧拼图拆解与动图精修工坊（Multi-frame & GIF Studio）**。

### 核心设计准则：
1. **单工作台流水线（No Broken Tabs）**：
   * 彻底摒弃割裂的多 Tab 模式，将「拆解 ➔ 预览 ➔ 删帧 ➔ 调速 ➔ 导出」整合在同一个主工作区中，一气呵成。
2. **随心输入，静默归档（Zero Output Friction）**：
   * 用户从任意位置选择图片（桌面、微信、下载区均可）；
   * 软件界面绝不强迫用户配置输出路径，所有成果全自动静默收纳至 exe 旁的 `output/`（若无权限则降级至用户图片目录）；
   * 界面提供显眼的【📂 打开成品目录】一键直达。
3. **真实过审级微信表情包预设**：
   * 针对微信自定义表情规范（硬指标：**文件大小 ≤500KB、长边 240px**），内置自适应调色板与智能压缩引擎，确保导出的动图 100% 能够直接添加进微信表情。
4. **所见即所得（WYSIWYG）**：
   * 切前：原图上实时叠加绘制网格分割虚线，切得准不准一目了然；
   * 切后：原地动图播放器流畅播放，循环模式（正序/乒乓）即预览即导出。

---

## 二、 界面原型设计（Wireframe）

```text
+-----------------------------------------------------------------------------------------------+
| 🎞️ GifCreater 动画工坊 v2.0                                                    [-] [口] [X]   |
+-----------------------------------------------------------------------------------------------+
| 📥 放入素材: [ C:/Pictures/turtle_16frames.png                       ] [ 📁 浏览图片... ] [ 📂 浏览帧目录... ] |
+---------------------------------------------------------------+-------------------------------+
| 【 🖥️ 视觉中心与实时播放器 】                                 | 【 ⚙️ 切片与动画预设 】        |
|                                                               |                               |
|   +---------------------------------------------------------+ | 🔲 网格切片:                  |
|   |                  大图预览 (带红虚线分割参考)             | |   网格: [ 4 ] 行 × [ 4 ] 列   |
|   |                             或                          | |   模式: (o) ✨ 智能去黑边     |
|   |             生成的动图原地流畅循环播放                  | |         ( ) 📐 绝对均匀切     |
|   +---------------------------------------------------------+ | ----------------------------- |
|     [ ▶ 播放/暂停 ] [⏮ 首帧] [⏭ 尾帧]  [进度条: 05/16]       | ⏱️ 动画节奏:                  |
|     循环: (o) 正常循环   ( ) 🔁 乒乓往复循环 (Boomerang)      | |   帧间隔: [ 350 ] ms        |
|                                                               | |   尾帧停留: [ 1500 ] ms     |
|                                                               | ----------------------------- |
|                                                               | 📦 导出预设目标:              |
|                                                               |   (o) 🌟 原画高清 GIF         |
|                                                               |   ( ) 💬 微信表情包 (≤500KB)  |
|                                                               |   ( ) ⚡ 高保真 WebP (体积减半)|
|                                                               |                               |
|                                                               |   [ 🚀 一键拆解并合成动图 ]   |
|                                                               |   [ 📂 打开成品目录 (output) ]|
+---------------------------------------------------------------+-------------------------------+
| 🎞️ 序列帧胶卷 (共 16 帧 | 支持选中废帧按 Delete 删除 | 支持双击查看大图)                      |
| [ #01 ]  [ #02 ]  [ #03 ]  [ #04 ]  [ 废帧❌右键删除 ]  [ #05 ] ... [ #16 ]                    |
+-----------------------------------------------------------------------------------------------+
| 就绪 | 当前素材: 1536 × 1024 像素 | 成品已自动收纳于 output/ 目录                             |
+-----------------------------------------------------------------------------------------------+
```

---

## 三、 核心模块与技术实现要点

### 1. 资产自动归档机制 (`output/`)
* 根目录结构：
  ```text
  output/
  ├── gifs/     # 动图成品归档
  └── frames/   # 拆解出的各组单帧文件夹
  ```
* 路径解析防坑策略：统一基于 `sys.executable`（冻结打包模式）或 `__file__`（开发模式）的绝对目录解析，防止通过右键启动时 CWD 漂移。
* 权限降级：若 exe 所在目录只读，优雅降级至 `%USERPROFILE%/Pictures/GifCreater/output`。

### 2. 动图实时播放器（Tkinter In-App Player）
* 架构：基于 `root.after()` 异步事件循环调度。
* 定时器安全防重入：在重载、切帧、改速或关闭时，必须调用 `after_cancel` 清理已排队的定时任务，避免回调累加造成加速死循环。
* 乒乓往复循环（Boomerang）：生成序列索引 `0, 1, ..., N-1, N-2, ..., 1`，并在导出时同样展开该序列。

### 3. 微信自定义表情规范压缩引擎
* 微信限制：硬指标 **长边 ≤240px、文件体积 ≤500KB**（非 1MB）。
* 自适应压缩策略：
  1. 画面等比缩放至最长边 240px；
  2. 适度提升播放速度至 80~120ms（表情包更灵动）；
  3. 采用 PIL 自适应调色板（Adaptive Palette，减少至 128 或 64 色）循环试探，确保输出严格控制在 500KB 安全线内。

### 4. 切前视觉辅助线（Visual Grid Overlay）
* 用户修改行列数（如 4×4、3×3）时，左侧画布在预览大图上实时叠加绘制清晰的分割参考线，用户一眼即可看清切割边界是否对准。

### 5. 交互式序列帧胶卷
* 拆解完成后，所有帧直接陈列在底部时间轴上；
* 选中某个废帧（如闭眼、AI 变形帧）可直接点击删除或右键删除，剩余帧立刻重新索引并实时联动给播放器与导出引擎。

---

## 四、 CI/CD 自动化构建与全自动 Release 架构设计 (v2.1)

### 1. 流水线分级双门禁
```text
Push (main) / Tag (v*) 
   │
   ▼
[Job 1: TDD & Coverage Gate] (windows-latest)
   ├── 安装环境与依赖 (requirements-dev.txt)
   └── pytest tests/ --cov=gif_tool --cov-fail-under=90
   │   └── 覆盖率 < 90% 立即熔断中断
   ▼
[Job 2: Build & Release] (needs: test)
   ├── PyInstaller 自动化构建免安装 Windows EXE
   ├── 自动压缩为 GifCreater-windows-x64.zip
   └── 全自动发布 Release (Latest Rolling Release)
```

### 2. 全自动 Latest 滚动发布策略 (方案 1)
* **免除人工打 Tag**：每次合并/推送至 `main` 分支，流水线自动覆盖更新名为 `latest` 的 GitHub Release；
* **永久固定下载直链**：
  - 压缩包：`https://github.com/HanboyLee/GifCreater/releases/download/latest/GifCreater-windows-x64.zip`
  - 单文件：`https://github.com/HanboyLee/GifCreater/releases/download/latest/GifCreater.exe`
* **双模兼容**：依然保留推送 `v*` Tag 创建里程碑历史版本归档的能力。
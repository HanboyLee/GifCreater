# 需求文档 (Requirements Document)

本文件用于统一记录、追踪与维护项目的所有产品需求及功能迭代。后续新增需求时，直接在此文档中进行追加与版本迭代更新。

---

## v2.0 需求基线 (已实现)

### 1. 核心业务需求
- [x] **多帧拼图自动/半自动切片**：
  - [x] 支持常见 4x4、3x3、6 帧长图等拼图切片。
  - [x] 自动探测黑边、白边与分割线，具备智能吸附算法。
  - [x] 提供可视化红色分割线交互拖拽微调，像素级调节行/列分界。
- [x] **实时尺寸与比例标注**：
  - [x] 实时计算切片单格像素尺寸与标准宽高比（如 1:1, 16:9, 9:16, 4:3, 3:2 等）。
- [x] **动图生成与播放**：
  - [x] 界面内置播放器，支持原地播放/暂停、滑动条控制、帧率调节。
  - [x] 支持常规正向循环与往复乒乓循环（Boomerang）。
- [x] **格式与尺寸规范导出**：
  - [x] 导出为 GIF 或 WebP。
  - [x] 提供「微信表情包模式」，强制约束长边尺寸与压缩体积（严格 <= 500KB）。
- [x] **帧管理能力**：
  - [x] 底部胶卷缩略图展示，支持单帧删除与恢复已删帧。

### 2. 工程与交付需求
- [x] **绿色单文件运行**：支持免安装单一可执行文件运行（Windows x64 EXE）。
- [x] **零配置本地输出**：生成过程切片与导出结果统一归档至本地目录。

---

## 质量门禁需求 (v2.1 规范引入) (已实现)

- [x] **测试驱动开发 (TDD)**：所有核心算法与功能逻辑均需具备可复现的单元测试用例。
- [x] **覆盖率硬指标**：核心业务模块单测覆盖率需达到 **90% 以上**。
- [x] **CI/CD 职责分立与版本 Tag 自动化发布体系**：
  - [x] **CI 持续集成门禁（推送至 main/PR 时触发）**：流水线自动执行文档基线同步校验、Ruff 静态语法 Lint 并阻断低于 90% 覆盖率的代码合入。
  - [x] **CD 持续交付与打包（推送 `v*` Tag 或手动调度触发）**：专责调用 PyInstaller 自动编译独立 Windows EXE、压缩 ZIP 并发布带有规范英文 Release Notes 的正式 GitHub Release，彻底终结每次 push 频繁打包浪费算力与版本混淆的缺陷。
  - [x] **支持随时手动按需构建（workflow_dispatch）**：开发者可在 GitHub Actions 界面一键手动触发全套测试与打包验证。

---

## v3.0 现代桌面客户端体验升级需求 (已实现)

### 1. 核心定位与设计诉求
- [x] 针对原有 Tkinter 客户端在 UI 表现力较弱、交互体验较单薄的痛点，在**坚持“100% 本机算力、零云端依赖、离线独立运行”**的根本前提下，全面重构 UI 展示层，打造媲美现代操作系统原生质感的专业动图工坊。

### 2. UI 视觉表现力需求
- [x] **现代化设计语言 (Fluent Design)**：
  - [x] 采用 Windows 11 风格的现代设计语言，引入亚克力 (Acrylic) / 云母 (Mica) 视觉层次；
  - [x] 全局控件统一具备现代圆角、层次阴影、悬停/点击微动效；
  - [x] 彻底解决高分屏 (High DPI / 4K / 2K) 缩放模糊与排版错位问题。
- [x] **主题自适应与切换**：
  - [x] 支持深色模式 (Dark Mode) 与浅色模式 (Light Mode)；
  - [x] 具备跟随操作系统主题自适应切换能力。

### 3. 高级交互体验需求
- [x] **流式画布与交互参考线**：
  - [x] 画布支持鼠标滚轮缩放与按住空格/中键拖拽平移大图；
  - [x] 网格切片参考线支持鼠标直接抓取、平滑拖拽微调，并在边缘提供磁吸辅助。
- [x] **专业级序列帧时间轴 (胶卷)**：
  - [x] 采用卡片式流式布局，支持鼠标横向滚轮浏览；
  - [x] 支持单选、Ctrl/Shift 多选废帧批量剔除，支持快捷键（Delete 键）删除；
  - [x] 废帧删除后提供“撤销/恢复”友好提示。
- [x] **丝滑的非阻塞异步体验**：
  - [x] 切片拆解、自适应调色板压缩、动图合成等重型计算必须在后台异步线程执行；
  - [x] 界面提供现代旋转环 (ProgressRing) 与实时步骤状态提示，**严禁任何操作导致界面“未响应”或假死**；
  - [x] 操作结果采用现代气泡通知 (InfoBar) 呈现，取代生硬阻塞的系统弹窗。

### 4. 架构与工程继承需求
- [x] **核心算法 100% 复用与本地化**：
  - [x] 严格继承 `gif_tool.py` 中经过严格测试的图像切片、去黑边、微信表情包 ≤500KB 压缩算法；
  - [x] 计算 100% 在本地 CPU 完成，不产生任何远程网络通信与数据外泄。
- [x] **面向未来演进的专案文件夹层级规范**：
  - [x] 不局限于当前代码，全面规划支撑未来迭代与功能扩展的标准文件夹体系；
  - [x] 聚焦高内聚、低耦合的**文件夹层级（Directory Level）**划分，涵盖文档中心 (`docs/`)、核心源码 (`src/`)、静态资源与主题 (`resources/`)、测试分类套件 (`tests/`)、工程脚本 (`scripts/`) 与本地归档 (`output/`)；
  - [x] 确立 **PyQt6 + PyQt-Fluent-Widgets** 作为现代 UI 统一技术基线；
  - [x] 彻底淘汰原 Tkinter 遗留代码，专案轻装前行；
  - [x] 采用渐进式落地策略：优先攻克高颜值 Fluent 交互界面、深浅色主题与异步任务，预留 `config/` 与 `i18n/` 目录骨架。
- [x] **质量门禁与交付体系**：
  - [x] 维持单测行覆盖率 ≥ 90% 的硬性门禁；
  - [x] 维持单文件 Windows EXE 绿色打包与 GitHub Actions 滚动发布机制。

---

## v3.1 平台预设与表情包配文增强需求 (已实现)

### 1. 表情包专属文字叠加 (Text Overlay & Transform)
- [x] **零侵入设计 (Optional)**：
  - [x] 文本输入框默认留空；留空时保持原画切片输出，0 额外计算开销；
  - [x] 用户输入文本时，原地播放器与导出渲染图层实时同步白字黑边预览。
- [x] **画布自由拖拽与九宫格对齐**：
  - [x] 鼠标在画布上可直接抓取文字图元自由拖动，像素级放置到画面任意角落；
  - [x] 支持 3x3 九宫格一键快捷对齐（顶部居中、底部居中、正中央等）；
  - [x] 坐标系统采用相对比例（0.0~1.0），确保多分辨率与导出尺寸下位置一致。
- [x] **任意角度旋转 (-180° ~ +180°)**：
  - [x] 支持连续角度滑块调节，带 0° 中心吸附；
  - [x] 提供 `-15°`（动感倾斜）、`0°`、`15°`、`45°` 常用表情包角度快捷按钮；
  - [x] 采用双三次插值抗锯齿旋转，杜绝字体边缘锯齿。
- [x] **色彩、透明度与边框深度定制**：
  - [x] **文字颜色与透明度**：支持 Fluent 拾色器及 0%~100% 透明度调节（支持半透明水印）；
  - [x] **描边轮廓定制**：支持描边颜色拾取、0~10px 描边粗细调节（0 为无边框）；
  - [x] **爆款风格一键模板**：内置经典黑白、动感荧光黄、高能爆红、透明水印等快捷模板。

### 2. 平台导出预设管理器 (Preset Selector)
- [x] **统一 Fluent 下拉预设选择器**：
  - [x] 升级现有的散落单选框为带有规格说明的高扩展性 ComboBox；
  - [x] 预设内置：
    - [x] `💬 微信表情包 (1:1)`：强制约束最长边 $\le 240\text{px}$、文件体积 $\le 500\text{KB}$（512,000 字节）、五阶调色板自适应衰减；
    - [x] `📱 小红书 / 社交动态 (3:4)`：竖版画幅自适应居中补充背景；
    - [x] `🌟 原画超清 GIF`：无损原始单格尺寸与 256 色量化；
    - [x] `⚡ 高保真 WebP`：体积减半的高压缩比现代网页格式。


---

## v3.4 交互细节与品牌视觉精细化规范 (已实现)

### 1. 动图导出帧间隔精度与全平台往复一致性规范 (v3.4.1)
- [x] **帧间隔与末尾停顿高保真输出 (Duration Fidelity)**：
  - [x] 导出引擎必须 100% 尊重用户在界面配置的帧间隔（`duration`，毫秒）与末尾停顿时间（`end_pause`，毫秒）；
  - [x] 严禁任何导出预设（包括微信表情包预设）进行任何形式的暴力截断（如强行限制在 120ms）或尾帧时间抹平；
  - [x] 支持标准的 GIF89a 规范量化对齐：将输入毫秒数对齐为 10ms 的整数倍（四舍五入），并设置严格的物理下限 $\ge 20\text{ms}$，防止浏览器和播放器发生 100ms 降级。
- [x] **全预设往复（Boomerang）行为一致性**：全平台各导出预设均支持往复动画生成。

### 2. 智能网格线投影波谷吸附与中心对齐规范 (v3.4.2)
- [x] **AI 拼图分镜缝隙自动居中 (Gutter Center Snapping)**：
  - [x] 针对 Midjourney / SD / Flux 生成的多格拼图、漫画分镜等非绝对等宽等高素材，切片引擎必须具备智能识别分镜留白缝隙（Gutter / Margin）的能力；
  - [x] 严禁简单依赖数学几何等分；必须通过投影积分分析，在理论等分点局部窗口内搜索低信息量/背景波谷，并将切线**绝对锁定在波谷带的物理中位线（Centerline）**，杜绝切线压在分镜边缘或人物手脚；
  - [x] 原生支持 RGBA 透明通道背景判定，透明分镜缝隙优先识别。

### 3. 官方品牌视觉标识与图标系统规范 (v3.4.3)
- [x] **统一品牌视觉心智 (Visual Identity)**：
  - [x] 确立「极简双态几何切片」官方品牌 Logo（2×2 模块切片与动图循环箭头咬合，青蓝至荧光绿 Fluent 渐变，钛黑磨砂圆角卡片底板）；
  - [x] 彻底摆脱 Windows 默认空白应用图标与 Python 脚本共享图标状态。
- [x] **Windows 工业级多分辨率适配 (Multi-Res ICO)**：
  - [x] 图标资产必须包含 `16×16`、`24×24`、`32×32`、`48×48`、`64×64`、`128×128`、`256×256` 完整 Mipmap 尺寸链，满足不同 DPI 缩放下的像素级清晰度；
  - [x] 支持 Windows 原生 `AppUserModelID` 独立任务栏进程绑定；
  - [x] PyInstaller 打包必须将官方图标内嵌为 `GifCreater.exe` 文件徽标。

---

## v3.5 Prompt 工作台与 Agent (已实现)

规格：`spec/v3.5_prompt_collector.md`。原型：`docs/prototype.html`。

### 1. 信息架构
- [x] 左侧导航三项：动图工坊 / Prompt / 设置
- [x] 动图工坊侧栏不出现 Prompt 卡片，不把 Prompt 写入配文
- [x] 表单控件有短名称；无步骤说明书、无具体生图产品名

### 2. v3.5.0 Prompt 工作台与本地收藏（离线）
- [x] **布局**：左栏收藏（搜索、列表、导入、导出）；右栏网格 + 提示 + Prompt + 复制/保存
- [x] **网格**：快捷 `2×2` / `3×3` / `1×6` / `4×4`；自定义行、列（1–20，与工坊切片一致）
- [x] **收藏库**：SQLite 主存（`output/prompts/library.sqlite`，权限降级同 `paths.py`）
  - [x] CRUD：选中编辑、保存、列表展示标题与网格
  - [x] 字段：标题、Prompt 正文、网格 `rows x cols`、时间戳；不在 UI 展示生图产品名
  - [x] JSON 导入合并、JSON/Markdown 导出
- [x] **复制**：InfoBar 反馈
- [x] 不配置 API 也可使用收藏与手写 Prompt；「完善」本小版本不发网

### 3. v3.5.0 设置 · 外观持久化
- [x] 设置页「外观」：主题 Radio **横排**（主题　深色　浅色）
- [x] 主题写入本地 JSON，启动恢复；与工坊顶栏切换同一数据
- [x] 网格 / 提示 / Prompt 不进设置

### 4. v3.5.1 设置 · API 与「完善」
- [x] 设置页「API」：Provider、Base URL、文本模型 ID、API Key（保存后显示「已保存」）、测试连接、清除 Key
- [x] Provider 预设含 OpenRouter（默认）、OpenAI、DeepSeek、Ollama、自定义；OpenRouter 为 OpenAI 兼容 `chat/completions`
- [x] Key 独立密文存储（不进 sqlite、不进明文 JSON、不进日志）；非机密与主题同 JSON
- [x] Prompt 页「完善」：按当前网格行×列生成自然语言分镜 Prompt，写入 Prompt 框；无 Key 则提示去设置
- [x] 配方：同一角色、拍数 = 行×列、时序连续、禁止 `--ar`/`--grid` 等参数
- [x] `QThread` 调用，可取消；失败 InfoBar
- [x] **异步大模型完善四重即时状态反馈闭环 (Compound Fluent Feedback)**：
  - [x] 按钮动态态变：点击后文字立即切换为「⏳ 正在完善...」并禁用防抖，请求结束/失败恢复为「完善」并重新启用；
  - [x] Fluent 流光进度条 (`IndeterminateProgressBar`)：调用期间展开呼吸流转，请求结束立即隐藏；
  - [x] 就地常驻状态提示行 (`CaptionLabel`)：调用时清晰显式呈现正在访问的目标模型名称（如 `deepseek/deepseek-v4.1-flash`），成功时提示完成，失败时就地显式保留具体错误原因（如认证失败、超时、网络断开），解决稍纵即逝痛点；未配 Key 时就地提示引导；
  - [x] 双保险通知联动：大模型调用完成或异常时同步触发系统级 InfoBar。
- [x] **分镜提示词背景模式与防越界穿模体系 (Background Modes & Anti-Bleed Architecture)**：
  - [x] **背景四态解耦切换**：界面与生成引擎彻底解耦「透明」与「纯色」概念，支持「透明背景 (表情包推荐)」、「纯色背景」、「场景背景 (连贯环境)」、「自由不限」四态独立切换，透明模式生成免抠贴纸提示词，纯色模式生成纯白平板实底；
  - [x] **60%~70% 紧凑角色体量硬锁**：代码提示词锁强制角色占格比例上限（60%~70%），留足 30%~40% 安全空域，根除主体过大问题；
  - [x] **15% 安全隔离护城河 (Safety Margin)**：强制四周预留 >=15% 空域缓冲区，严禁肢体、武器与特效跨格溢出 (NO BLEED-OVER)；
  - [x] **大模型 Action 动作就地收敛约束**：系统提示词强制动作就地紧凑收敛，严禁大跨度位移，透明底时严禁生成环境道具。
- [x] 本里程碑不调用生图 HTTP、不上传本地图片

### 5. 非目标（本里程碑不做）
- [ ] ~~工坊内直连生图~~（明确不做）
- [ ] ~~第二输入框「继续改」~~（明确不做）
- [ ] ~~Agent 历史表~~（首版不做）

---

## 需求变更与迭代记录 (Changelog)
- **2026-09-20 (背景模式四态解耦与概念修正)**：修正将透明与纯色混同为“纯色透明底”的逻辑缺陷，正式解耦为「透明背景 (表情包推荐)」、「纯色背景」、「场景背景」与「自由不限」四态，保证交互概念与提示词语义精确严谨。
- **2026-09-20 (分镜提示词背景模式与防穿模安全护城河规范升级)**：设计并落地背景模式受控选择，同时在代码锁中注入 60%~70% 紧凑角色比例与 15% 安全护城河硬规则，彻底根除切片跨格串图与背景杂质痛点。
- **2026-09-20 (Prompt 工作台「完善」异步状态多维反馈升级)**：依据 TypeSafe Jev 100% 裁决，重构「完善」调用链路，引入按钮态变、原生 Fluent 流光进度条、就地模型名称感知与常驻错误回显四重反馈体系，彻底终结黑盒等待痛点。
- **2026-09-20 (v3.5.0 正式交付)**：正式交付 Prompt 工作台整页、SQLite 收藏库（JSON/MD 导入导出）、横排主题持久化、OpenRouter 兼容自然语言分镜完善与 Windows DPAPI 独立密钥存储；集成 TypeSafe Jev 语义决策与验车门禁。
- **2026-09-18 (CI/CD 职责分立与版本 Tag 驱动发布演进)**：确立 CI（日常提交毫秒级静态分析与测试门禁）与 CD（打版本 Tag 触发 EXE 打包发布）职责分立原则，关闭日常 main 提交频繁打包的高耗能模式，实现版本发布确定性与资源节约。
- **2026-09-18 (需求基线全景 Checklist 化与 v3.1 交付状态确认)**：全面升级需求文档为 GitHub 规范的任务检查清单 (Checklist) 格式，正式确认并标记「v3.1 平台预设与表情包配文增强」为已实现。
- **2026-09-20 (Prompt 工作台体验修补)**：配文字号可调；均匀等分关闭智能去缝；完善 Prompt 禁止画网格线；切片按格宽去粗缝；设置页模型搜索下拉、Key 显隐、仅保存。
- **2026-09-18 (Prompt 工作台 v3.5.0/v3.5.1 已实现，基线仍为 v3.4.3)**：三页导航、SQLite 收藏、主题持久化、OpenRouter 兼容完善、DPAPI 存 Key。未升正式基线号。
- **2026-09-18 (v3.4.3 官方品牌图标与 Windows 桌面原生挂载)**：确立「极简双态几何切片」官方品牌 Logo，生成多尺寸工业级 ICO 资产并挂载至窗口与任务栏，内嵌至 PyInstaller EXE 打包。
- **2026-09-18 (v3.4.2 智能网格线波谷居中吸附)**：重构网格线探测引擎为投影能量积分波谷中位线算法，打通 UI 自动吸附链路与一键重新对齐，彻底消除手动微调负担。
- **2026-09-18 (v3.4.1 帧间隔保真与微信往复修复)**：确立全预设帧间隔高保真规范，彻底移除微信表情包 120ms 强制截断与末尾停顿抹平逻辑，全平台统一支持 Boomerang 往复。
- **2026-09-18 (v3.4 统一主题系统与高对比度设计令牌)**：新增集中式主题管理系统 `ThemeManager` 与暗黑/明亮 QSS，消除深色模式黑字缺陷。
- **2026-09-18 (v3.3 自适应与滚动条避让)**：引入弹性工作区分割器与复合网格流，消除滚动条遮挡。
- **2026-09-18 (v3.1 & v3.5+ 立项)**：正式将「平台预设与表情包配文增强（v3.1）」列入开发基线；正式立项「Prompt 收集器与 Agent 生图工作流引擎（v3.5+）」，打通平台从 0 到 1 完整作品生态。
- **2026-09-18 (v3.0.0 正式交付)**：完成 PyQt6 Fluent 表现层重构、无头核心算法解耦、67项 TDD 单测（覆盖率 97.31%）、旧版 Tkinter 清理与 PyInstaller 独立单文件打包验证。
- **2026-09-17 (v3.0.0 规划阶段)**：更新面向未来全生命周期的专案文件夹层级架构规划（覆盖资源、国际化、配置预设、工程脚本及测试分类），更新 PRD 与设计文档。
- **2026-09-17 (v3.0.0 规划阶段)**：发起 UI 展示层与交互体验重构规划，确立纯本地桌面客户端演进路线，更新 v3.0 PRD 需求基线。
- **2026-09-11 (v2.1.1)**：支持 main 分支全自动滚动发布 Latest Release，彻底免除手动打 Tag 流程。
- **2026-09-11 (v2.1.0)**：初始化统一文档管理规范，建立质量门禁需求与 CI/CD 自动化发布流水线需求。



